import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel handles routing automatically - no need for 'export' mode
  // This allows Vercel's Edge Network to properly route all requests
};

export default nextConfig;
